{
  "name": "macvasquez-payments",
  "version": "1.0.0",
  "private": true,
  "description": "Pagos PayPhone via backend serverless (sin exponer secretos)",
  "type": "module",
  "scripts": {
    "dev": "vercel dev",
    "build": "echo \"No build step\"",
    "start": "vercel dev"
  }
}
